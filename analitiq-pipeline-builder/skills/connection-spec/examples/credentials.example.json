{
  "$schema": "https://schemas.analitiq.ai/connection/latest.json",
  "alias": "sftp_partner_drop",
  "connector_alias": "sftp",
  "display_name": "Partner SFTP drop",
  "parameters": {
    "host": "sftp.partner.example",
    "port": 22,
    "username": "data_ingest"
  },
  "secret_refs": {
    "ssh_private_key": "secrets/sftp_partner_drop/ssh_private_key",
    "ssh_known_hosts": "secrets/sftp_partner_drop/ssh_known_hosts"
  }
}
