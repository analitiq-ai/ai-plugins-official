{
  "connector_name": "CreditSafe",
  "connector_type": "api",
  "connector_descr": "Business credit reporting and risk management",
  "slug": "creditsafe",
  "base_url": "https://connect.creditsafe.com/v1/",
  "auth": { "type": "api_key" },
  "headers": {
    "Content-Type": "application/json",
    "Authorization": "Bearer ${auth_token}"
  },
  "form_fields": [
    { "name": "username", "label": "Username", "type": "text", "required": true },
    { "name": "password", "label": "Password", "type": "password", "required": true }
  ],
  "post_auth_steps": [
    {
      "step_order": 1,
      "field_name": "auth_token",
      "type": "auto",
      "token_expiry_seconds": 3600,
      "source": {
        "method": "POST",
        "url": "https://connect.creditsafe.com/v1/authenticate",
        "headers": { "Content-Type": "application/json" },
        "body": {
          "username": "${username}",
          "password": "${password}"
        },
        "value_path": "token"
      }
    }
  ],
  "timeout": 30,
  "requests_per_second": { "max_requests": 60, "time_window_seconds": 60 }
}
